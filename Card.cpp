#include "Card.h"

Card::Card(std::string cardNumber, std::string expirationDate, std::string securityCode) : m_cardNumber(cardNumber), m_expirationDate(expirationDate), m_securityCode(securityCode) {}
